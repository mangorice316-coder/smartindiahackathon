# ==============================================================================
# LRIDS Frontend Environment Configuration
# ==============================================================================

# API Base URL (leave blank to use Vite reverse proxy /api/v1 during development)
# In production with separate hosting, point to your FastAPI server:
# VITE_API_BASE_URL="http://localhost:8000/api/v1"
